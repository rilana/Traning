@model IEnumerable<WatcherModel.Manager>

@{
    ViewBag.Title = "Manager";
}

<h2>Manager</h2>

<p>
    @if (User.IsInRole("admin"))
    {@Html.ActionLink("Create New", "Create")}
</p>
<div>
    @using (Ajax.BeginForm("SearchManager", new AjaxOptions { UpdateTargetId = "searchresults" }))
    {
        <input type="text" name="name" />
        <input type="submit" value="Поиск" />
    }
    @Html.Partial("SearchManager")
</div>